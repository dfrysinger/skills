[AUTOREVIEW-DAEMON-SESSION:96efca49-7380-4494-86c4-ab4ab954ee3f]

You are running as an unattended background daemon (macOS launchd), NOT in an
interactive session. There is no human to ask. Execute the skill-curator daily
"tick" autonomously, then exit. The sentinel token on the first line marks this
as a daemon-origin session so future skill-review sweeps skip it — do not remove
it. This is a complete runbook; you have no custom instructions loaded.

This is a DRY-RUN gate. It NEVER mutates, archives, or deletes any skill file and
NEVER pushes git. Its only side effects are writing a report and updating the
curator state file, both under ~/.copilot/skill-state/ (outside the repo).

SEQUENCE (follow ~/code/skills/skills/skill-curator/SKILL.md tick mode):
1. Halt switch. If ~/.copilot/skill-state/skill-review/disable-daemon exists,
   print `DREAM_PASS_RESULT: aborted halt-switch` and STOP.
2. Interval gate. If DREAMING_ORCHESTRATED=1, the outer orchestrator already
   owns cadence and you MUST skip this gate. Otherwise run
   ~/code/skills/skills/skill-curator/scripts/should-run-now.sh.
   If it exits non-zero, print the one-line skip reason from stderr, then print
   `DREAM_PASS_RESULT: ok skipped=cadence-not-due` and STOP.
3. If the gate passes (>= 7 days since last run, not paused), run the full
   --dry-run workflow from the skill:
   - Print the dry-run banner (references/dry-run-banner.md).
   - Load the verbatim CURATOR_REVIEW_PROMPT from references/curator-prompt.md.
   - Identify prefix clusters across BOTH skill roots — public ~/code/skills/skills/ and local ~/.copilot/skills/ (scripts/list-clusters.sh scans both).
   - Produce a markdown report ending with the structured YAML block
     (consolidations + prunings). Respect the tiered-authority rule: agent-created
     skills may be proposed for consolidation/pruning; hand-made skills are
     recommend-only (manual_review list).
4. Save the report via scripts/curator-report.sh — do NOT mutate any skill file.

Finish by writing a one-paragraph summary (counts of proposed consolidations /
prunings, or the skip reason) to stdout for the daemon log, then exit. Do not
apply anything — a human runs `/skill-curator --live` to apply. The LAST line
must be `DREAM_PASS_RESULT: ok consolidations=<N> prunings=<M>`. On any guard,
prompt, report, or state failure, print
`DREAM_PASS_RESULT: aborted <one-line reason>` instead.
