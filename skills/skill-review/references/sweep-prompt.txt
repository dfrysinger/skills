[AUTOREVIEW-DAEMON-SESSION:96efca49-7380-4494-86c4-ab4ab954ee3f]

You are running as an unattended daily background daemon (macOS launchd), NOT in
an interactive session. There is no human to ask. Execute the skill-review
"sweep" autonomously and conservatively, then exit. The sentinel token on the
first line marks this as a daemon-origin session so future sweeps skip it — do
not remove or echo it into any skill file.

Run the skill-review sweep exactly per
~/code/skills/skills/skill-review/SKILL.md ("sweep" mode) and
~/code/skills/skills/skill-review/references/review-prompt.md (the verbatim
Hermes _SKILL_REVIEW_PROMPT + Copilot execution contract). That execution
contract is BINDING. This is a complete runbook; you have no custom instructions
loaded.

TWO-ROOT MODEL (read first):
- The daemon writes ONLY to the LOCAL native skills root ~/.copilot/skills
  (a local git repo with NO remote — nothing is ever pushed) and the shared
  state dir ~/.copilot/skill-state/skill-review (ledger + tombstones).
- The PUBLIC plugin repo ~/code/skills is curated/recommend-only. The daemon
  MUST NOT modify it: no pull, no commit, no push, no plugin.json registration.
  Native local skills need no registry entry — Copilot CLI loads them directly.
- Scripts are invoked from the public repo plugin (read-only use); their writes
  target the local root + state dir via SKILLS_LOCAL_ROOT / SKILLS_STATE_DIR
  (defaults already point there).

SEQUENCE:
1. Pre-flight. Stop if ~/.copilot/skill-state/skill-review/disable-daemon exists
   (halt switch), printing `DREAM_PASS_RESULT: aborted halt-switch` before
   exiting. Confirm the LOCAL root exists at ~/.copilot/skills. Capture the
   public-repo baseline with
   `~/code/skills/skills/skill-review/scripts/verify-repo-unchanged.sh snapshot`.
   Then
   SNAPSHOT pre-existing dirty paths via
   `git -C ~/.copilot/skills status --porcelain` and remember them — they
   represent OTHER skills (or scratch the user dropped in) and are OUT OF
   SCOPE for this run. The daemon MUST NOT modify, commit, stage, or remove
   any path in that snapshot; it just steps around them. DO NOT halt on a
   dirty tree — whole-tree cleanliness is NOT a precondition. The daemon's
   scope is per-target skill, not repo-wide. Do NOT inspect or require the
   public repo to be clean.
2. Score window. The scoring window is a FIXED 14-day lookback, NOT the ledger
   watermark — a near-now watermark must never shrink the window and orphan
   older unreviewed sessions. Generate the query with
   ~/code/skills/skills/skill-review/scripts/score-sessions.sh (no timestamp arg
   — it defaults to the 14-day lookback) and run it via session_store_sql to
   rank recent sessions.
3. Dedup (per-session, NOT watermark). The query already excludes daemon-origin
   sessions (the sentinel above). You MUST additionally skip any session already
   reviewed, checked one-by-one via
   ~/code/skills/skills/skill-review/scripts/review-ledger.sh has <session_id>.
   Per-session ledger dedup — not a high watermark — is what prevents re-review,
   so the 3/run cap drains the backlog highest-score-first over multiple days.
4. Review. For each high-signal unreviewed session ABOVE the skill's threshold,
   load references/review-prompt.md and run a review pass: decide whether a NEW
   agent-created skill or a PATCH to an existing agent-created skill is
   warranted. Before creating, ALWAYS run check-tombstone.sh <candidate-name>
   (honor tombstones) and search BOTH roots for an existing umbrella to patch
   instead.
5. Apply (LOCAL root only). For each chosen target skill <name>:
   a. PER-TARGET cleanliness check: if `git -C ~/.copilot/skills status
      --porcelain -- <name>/` is non-empty (i.e., <name>/ ITSELF has
      uncommitted changes, either tracked-modified or untracked), SKIP this
      target — the user (or a prior session) may be editing it. Log the skip
      and move on. Other dirty skills in the tree are out of scope and do not
      affect this target.
   b. Create/patch by editing files directly under ~/.copilot/skills/<name>/
      (no slash commands in headless mode): write SKILL.md, mark new skills
      with mark-agent-created.sh, and validate with validate-skill.sh. Do NOT
      call registry.sh — native skills need no plugin entry.
6. Guard. Run two guards and abort the run if either fails:
   - ~/code/skills/skills/skill-review/scripts/verify-repo-unchanged.sh check —
     the public repo must match its pre-run baseline (the daemon must never
     touch it, but unrelated pre-existing human work is allowed).
   - ~/code/skills/skills/skill-review/scripts/verify-diff-scope.sh — local
     changes must stay within <name>/**, .archive/**, README.md. On any
     violation run the UNWIND procedure (contract item 9 of
     ~/code/skills/skills/skill-review/references/review-prompt.md) and abort.
     UNWIND reverts this run's commits and path-restores its uncommitted
     paths, leaving the user's pre-existing dirty paths from the pre-flight
     snapshot untouched. Capture `PRE_RUN_HEAD` of each root before the first
     action — UNWIND cannot run without it.
7. Commit (LOCAL only). Commit EACH action as its own reversible git commit
   in ~/.copilot/skills (co-authored-by Copilot). ALWAYS stage with a
   path-scoped `git add <name>/` — NEVER `git add -A` or `git add .`, which
   would sweep up the pre-existing dirty paths from the pre-flight snapshot.
   NEVER push (there is no remote).

   Ledger semantics (ONLY ledger sessions with a FINAL outcome):
   - created / patched: ledger as such after the commit lands.
   - skipped (reviewer judged no skill opportunity): ledger as skipped.
   - blocked-by-dirty-target (step 5a): DO NOT LEDGER this session. Leave it
     un-ledgered so the next sweep retries the same session once the target
     skill goes clean again. Log the block to stderr/run-log only. The
     fixed 14-day window + per-session dedup guarantees no loss.
   - blocked-by-validator (step "If validate-skill.sh fails"): same — DO NOT
     LEDGER; the next sweep retries. The reverted skill is not a final
     outcome.

HARD CAPS (unattended safety — do not exceed without a human):
- Review at most 3 sessions this run (highest-scoring first); leave the rest for
  tomorrow (per-session ledger dedup + the fixed 14-day window guarantee they are
  not lost — they resurface next run until reviewed or they age out of 14 days).
- Create at most 2 NEW skills this run. Patches to existing agent-created skills
  are not capped but must stay surgical.
- If validate-skill.sh fails on something you created, revert that skill and skip
  it — never commit an invalid skill.
- When in doubt, SKIP. The bar is high: class-level umbrella skills, not
  one-session micro-skills. A no-op run is an acceptable outcome for a daemon.

Finish by writing a one-paragraph summary of what was created / patched /
skipped to stdout (it is captured in the daemon log). Then, as the LAST line of
output, print exactly one status sentinel — the daemon reads this line to decide
whether the run succeeded, because the session footer only proves the process
stopped, not that the work was done:

  DREAM_PASS_RESULT: ok created=<N> patched=<M> reviewed=<K>

Print `DREAM_PASS_RESULT: aborted <one-line reason>` instead whenever a guard
fired, UNWIND ran, or you stopped early. A clean no-op run is still `ok` with
zeros.
